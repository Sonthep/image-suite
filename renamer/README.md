# Renamer

This submodule contains the batch image renaming utilities.

Planned features:
- Bulk rename by pattern (date, sequence, metadata)
- Preview rename operations before applying
- Undo/restore support (via transaction or backup)

Development notes:
- CLI and GUI entry points will be provided
- Tests should cover filename pattern parsing and metadata extraction

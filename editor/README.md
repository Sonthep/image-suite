# Editor

This submodule will contain basic image editing features.

Planned features:
- Crop, resize, rotate
- Basic color adjustments (brightness, contrast)
- Batch operations (apply preset to multiple images)

Development notes:
- Provide a lightweight UI component reusable by other features
- Keep dependency footprint small
